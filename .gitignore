node_modules/
.next/
out/
build/
dist/
coverage/

# Everything env-shaped is secret except the template. A narrower pattern let
# a .env.local.bak slip through once — this one cannot.
.env*
!.env.example

.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

*.tsbuildinfo
next-env.d.ts

storage/
tmp/

# Per-machine Claude Code settings (permission grants, local overrides).
# .claude/launch.json is shared on purpose; this file is not.
.claude/settings.local.json

legacy/**/node_modules/
